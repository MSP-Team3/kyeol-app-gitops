apiVersion: apps/v1
kind: Deployment
metadata:
  name: storefront
spec:
  template:
    spec:
      containers:
        - name: storefront
          env:
            - name: NEXT_PUBLIC_API_URI
              value: "https://api-prod.kyeol.click/graphql/"
