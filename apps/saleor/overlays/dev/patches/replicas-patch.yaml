# Saleor DEV Overlay: Replicas/Resources Patch
# DEV environment resource settings

apiVersion: apps/v1
kind: Deployment
metadata:
  name: storefront
  namespace: kyeol
spec:
  replicas: 1  # DEV uses 1 replica
  template:
    spec:
      containers:
        - name: storefront
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 384Mi
