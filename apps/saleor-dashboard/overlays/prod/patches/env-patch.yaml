apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashboard
spec:
  template:
    spec:
      containers:
        - name: dashboard
          env:
            - name: API_URI
              value: "https://api-prod.kyeol.click/graphql/"
