apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashboard
spec:
  template:
    spec:
      containers:
        - name: dashboard
          env:
            - name: API_URL
              value: "https://api.kyeol.click/graphql/"
            - name: API_URI
              value: "https://api.kyeol.click/graphql/"
